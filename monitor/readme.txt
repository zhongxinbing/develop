性能监控网页优化：

1.去线图中只显示 qor路径下存在的日期、但是保存的json中有所有日期的数据，

并且添加一个功能、用户可以自己选择在曲线图中显示那些日期的数据，曲线图中默认显示qor下的日期的数据

数据会随着时间的增加而增加，默认显示最近的51个数据，其他数据一直保留

添加一个功能：指定那些天显示在曲线图上

2.指定某两天的数据对比

   2. 1.误差范围可以指定百分比、可以执行差值

    2.2.保存对应case的误差范围配置，

    2.3.在跳转到compare页面时显示上一次的误差数据

3.多线程之间的对比的曲线图（我会提供数据、请给我一个json结构就行）

    每个线程上的

    线程之间的

4..优化数据结构

6.每个case定制对比策略

7.配置文件修改成json格式的