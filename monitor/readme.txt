性能监控网页优化：

1.去线图中只显示 qor路径下存在的日期、但是保存的json中有所有日期的数据，

并且添加一个功能、用户可以自己选择在曲线图中显示那些日期的数据，曲线图中默认显示qor下的日期的数据

数据会随着时间的增加而增加，默认显示最近的51个数据，其他数据一直保留

添加一个功能：指定那些天显示在曲线图上

2.指定某两天的数据对比

   2. 1.误差范围可以指定百分比、可以执行差值

    2.2.保存对应case的误差范围配置，

    2.3.在跳转到compare页面时显示上一次的误差数据

3.多线程之间的对比的曲线图（我会提供数据、请给我一个json结构就行）

    每个线程上的

    线程之间的

4..优化数据结构

6.每个case定制对比策略

7.配置文件修改成json格式的

/////  20260516
请根据以上代码。实现以下需求,并修改我给的文件中原本的代码：
    1.在单线程的曲线图中添加显示线程数为： 2 4 6 8 16 24 32 64 128 的曲线图；
        1.1默认显示0线程的；0线程的数据是当为total.json中的数据
        1.2其他线程数用户可以自己选择显示
    2.优化数根据添加的多线程修改数据结构 ：total.json
//// 20260516
请根据以下需求在不改变基本功能的情况下修改我给的代码：
1. main.tcl 中使用卡片网格（Grid + 悬浮效果）实现工具的显示：elint/ecdc等
2. 将main.tcl中单线程多线程的选择放在 elint_signal.html 中；通过侧边栏来实现进行选择
    2.1 侧边栏内容是
        （1）时间内存曲线图
            当点击该栏时；在右边显示如下顺序显示：
                显示刷新时间和统计的数据：阶段数:、天数、平均Runtime
                显示：qor case的选择器；阶段/rule的选择器；阶段搜索框
                显示曲线图日期显示（用户可以自定义在曲线图中显示日期）
                曲线图
                数据统计等
        （2）线程曲线图
            当点击该栏时；在右边显示如下顺序显示：
                显示刷新时间和统计的户数：：阶段数:、天数、平均Runtime
                显示：qor case的选择器；阶段/rule的选择器；阶段搜索框
                显示曲线图日期显示（用户可以自定义在曲线图中显示日期）；默认显示最新的日期
                曲线图：
                    runtime：
                        x轴：线程数
                        Y轴：时间
                    memory：
                        x轴：线程数
                        Y轴：内存
                数据统计等
        （3）数据对比
    2.2 点击单线程显示 elint_signal.html 中的内容
    2.3 点击多线程显示多线程曲线图
    将 elint_signal.html 修改成elint.html
3. 点击main.tcl中的elint则跳转到elint.html中、其他的类似
4. main.tcl中显示的工具有变量 CASE_CONFIG 中的键控制
5. 如果你有更好的建议也请给出
6. 为所有代码给出注释


我的需求是：
在elint.html中需要一个侧边栏；
    侧边栏内容是
        （1）时间内存曲线图
            当点击该栏时；在右边显示如下顺序显示：
                显示刷新时间和统计的数据：阶段数:、天数、平均Runtime
                显示：qor case的选择器；阶段/rule的选择器；阶段搜索框
                显示曲线图日期显示（用户可以自定义在曲线图中显示日期）
                曲线图
                数据统计等
        （2）线程曲线图
            当点击该栏时；在右边显示如下顺序显示：
                显示刷新时间和统计的户数：：阶段数:、天数、平均Runtime
                显示：qor case的选择器；阶段/rule的选择器；阶段搜索框
                显示曲线图日期显示（用户可以自定义在曲线图中显示日期）；默认显示最新的日期
                曲线图：
                    runtime：
                        x轴：线程数
                        Y轴：时间
                    memory：
                        x轴：线程数
                        Y轴：内存
                数据统计等
        （3）自定义性能对比

在对比的结果中增加 runtime/memory平均增加、Runtime/memory最大增加、Runtime/memory最大减少、runtime/memory平均变化。将鼠标放在对应的卡片上是，复现出排序前10的阶段/rule